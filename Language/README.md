# App languages

English is the default application language and is compiled from `Language/en`.

Add future translations in a sibling directory with Android resource qualifiers. For example, Simplified Chinese strings belong in `Language/zh-rCN/values-zh-rCN/strings.xml`; then register that language directory in `app/build.gradle.kts`.
