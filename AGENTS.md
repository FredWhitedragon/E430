# AGENTS.md

## 1. 项目目标与适用范围

本项目是使用 **Kotlin** 开发、在 **Android Studio** 中维护的轻量化 e621/e926 原生 Android 客户端。

本文件为仓库根目录的 Codex 工作约定，适用于整个项目。更深层目录中的 AGENTS.md 可补充局部约定；用户当前明确要求优先于本文件。

必须遵守的项目要求：

- 使用 Kotlin 编写应用代码，使用 Gradle Kotlin DSL 编写构建配置。
- **按照业务功能分包**，业务内部再按职责划分，不以全局技术层作为项目的主要组织方式。
- 以体积、启动速度、内存、流量和代码可维护性为轻量化目标。
- 优先完成可运行的业务闭环，再根据真实需求增加功能和抽象。
- 修改已有项目时，先阅读代码和构建配置，保留已经合理且稳定的实现。

本文件不定义接口协议。实现联网业务前，阅读仓库中的 `E621_API.md`。请求约定、认证方式、接口参数、返回结构和服务端限制均以独立接口文档及经核实的实际行为为依据，不在本文件重复维护。

## 2. 默认技术选择

以下是新项目的默认选择，不是对已有项目进行整体重写的理由。依赖版本应在初始化时根据官方兼容性说明确定并锁定，不使用动态版本，不凭记忆宣称某版本为最新版。

| 领域 | 默认选择与边界 |
| --- | --- |
| 语言与构建 | Kotlin、Gradle Kotlin DSL、Gradle Wrapper、Version Catalog |
| UI | Jetpack Compose、Material 3；新功能默认使用原生界面 |
| 页面组织 | 单 Activity；采用与项目依赖兼容的 Jetpack Navigation 稳定方案，只保留一套导航体系 |
| 状态管理 | AndroidX ViewModel、不可变 UiState、StateFlow、单向数据流 |
| 异步 | Kotlin Coroutines、Flow、结构化并发 |
| 网络 | OkHttp + Retrofit；复用客户端实例 |
| 序列化 | kotlinx.serialization；不同时引入多个 JSON 库处理同一职责 |
| 图片 | Coil；统一配置图片加载器、缓存和请求尺寸 |
| 简单设置 | DataStore；敏感凭据采用独立安全存储实现 |
| 依赖注入 | 默认构造函数注入和手动 AppContainer，规模需要时再评估 DI 框架 |
| 测试 | JUnit、kotlinx-coroutines-test；网络边界按需使用 MockWebServer，关键 UI 使用 Compose 测试 |

补充要求：

- 新项目默认先使用一个 `app` Gradle 模块，以包维持业务边界；不要为了形式提前拆成大量模块。
- 不默认引入 Room、WorkManager、复杂 MVI 框架、通用事件总线或完整 Clean Architecture 模板。
- 有明确的结构化离线数据、可靠后台任务或复杂分页需求时，再分别评估 Room、WorkManager、Paging 等工具。
- 不强制每个 Repository 配一个接口、每次调用配一个 UseCase。存在测试替身、多个实现或真正复用的业务规则时再抽象。
- `minSdk`、`compileSdk`、`targetSdk`、JDK、AGP 和 Kotlin 版本优先沿用已有项目；新项目选择相互兼容的稳定组合，并在 README 中记录。
- 不擅自改动用户的 Android Studio、系统代理、全局 Gradle、JDK 或环境变量配置。

## 3. 按业务分包

根包名使用项目确定的 namespace。下面以 `<base>` 表示，实际创建时必须替换，不能保留为字面包名。

| 包路径 | 责任 |
| --- | --- |
| `<base>.app` | Application、Activity、应用装配、AppContainer、应用级导航 |
| `<base>.posts` | 内容列表、内容详情及该业务的数据访问和模型 |
| `<base>.search` | 搜索输入、搜索条件、建议和历史等实际需要的搜索能力 |
| `<base>.favorites` | 收藏相关页面、状态及数据操作 |
| `<base>.account` | 账户相关页面、会话状态及账户数据操作 |
| `<base>.settings` | 应用设置、偏好项和缓存管理入口 |
| `<base>.core.network` | HTTP 客户端装配、通用传输错误、网络基础设施 |
| `<base>.core.storage` | 通用本地存储和安全存储基础设施 |
| `<base>.core.ui` | 主题与真正跨业务复用的无业务 UI 组件 |
| `<base>.core.common` | 少量无业务归属的基础类型；不得成为杂物目录 |

这些业务包是可选的组织示例，**不是要求一次性实现全部功能**。只为当前交付的功能创建包和文件，暂未实现的业务不创建空壳目录。

业务规模较小时，可直接将少量文件放在业务包中；随着文件增多，再建立业务内的 `ui`、`data`、`model` 等子包。例如：

| 示例路径 | 内容 |
| --- | --- |
| `posts/ui/list/PostListScreen.kt` | 列表 UI |
| `posts/ui/list/PostListViewModel.kt` | 列表状态与用户操作处理 |
| `posts/ui/list/PostListUiState.kt` | 列表页面状态 |
| `posts/ui/detail/PostDetailScreen.kt` | 详情 UI |
| `posts/data/PostRepository.kt` | 内容业务的数据入口 |
| `posts/data/remote/PostService.kt` | 内容业务的远程访问声明 |
| `posts/data/remote/PostDto.kt` | 内容业务的网络 DTO |
| `posts/model/Post.kt` | 内容业务对外提供的应用模型 |

不要建立全局 `activities/`、`viewmodels/`、`repositories/`、`models/`、`adapters/` 来混放所有业务。业务 Service、DTO 和 Repository 也不应全部堆入 `core.network`。

## 4. 依赖边界与复用

- 依赖方向为：应用装配层连接业务；业务可使用 `core`；`core` 不依赖任何具体业务包。
- UI 经 ViewModel 调用 Repository；不要在 Composable 或 Activity 中直接发送网络请求、读取存储或执行复杂业务逻辑。
- Repository 协调业务数据访问、映射和缓存；ViewModel 处理页面状态与操作，不承担 HTTP 协议解析。
- 跨业务协作优先通过导航参数、明确的公共模型或 Repository 能力完成，禁止直接操作另一业务的 ViewModel 或页面私有状态。
- 业务之间不得形成循环依赖。确需共享时，明确能力的归属并暴露最小边界；必要时提取有业务名称的共享包，不把业务模型一律移入 `core.common`。
- 列表与详情共享同一业务的数据来源；跨页面的收藏等状态应由统一的数据入口维护，避免多个独立布尔值相互失同步。
- 导航通常只传 ID、搜索条件等小参数，不传大图、完整列表或敏感数据。
- DTO 留在数据访问边界，UI 使用应用模型。只做有价值的模型分离，不为同一份数据机械建立多层完全相同的对象。
- 可复用组件通过数据和回调工作，不自行查找全局依赖或获取业务 ViewModel。

上述 UI、Repository、单向数据流和生命周期处理原则参考 [Android 官方架构建议](https://developer.android.com/topic/architecture/recommendations)，具体分包方式和轻量化取舍以本项目约定为准。

## 5. Kotlin 与 Compose 编码规范

- 遵循 Kotlin 官方风格和仓库现有格式化设置；新项目默认 4 空格缩进、UTF-8、统一换行，不进行与任务无关的全仓格式化。
- 类和 Composable 使用 PascalCase，函数和属性使用 camelCase，包名全小写；避免无意义缩写。
- 优先使用 `val`、不可变集合接口、明确的可空类型和小型 data class。
- 不使用 `!!` 掩盖可空性，不滥用 `lateinit`，不以空 catch 吞掉错误。
- 不在生产代码中留下伪实现、占位成功结果或尚未实现的可点击按钮。
- ViewModel 向外暴露只读 `StateFlow`，可变状态保持私有；状态更新必须考虑并发。
- 使用生命周期感知的方式收集状态，例如 `collectAsStateWithLifecycle()`。
- Composable 的重组不得直接触发请求；副作用使用合适的 effect，并明确 key 与取消时机。
- 不在 ViewModel 中持有 Activity、View 或生命周期不匹配的对象。
- 使用合适的协程作用域；禁止 `GlobalScope`，禁止在主线程执行阻塞 I/O、图片处理或大规模计算。
- 不吞掉 `CancellationException`；取消页面、切换条件或退出账户时，相关旧任务应能终止。
- 公共类型和不直观的规则补充简短 KDoc 或注释；注释说明原因和边界，不逐行复述代码。
- 与用户沟通、实现说明及必要注释默认中文；代码标识符采用清晰英文。
- 可见文案放入 Android 资源，默认提供完整中文文案；技术名词和内容本身不强制翻译。

## 6. 页面状态与基本体验

- 数据页面明确区分首次加载、正常内容、空结果、刷新、加载更多和错误状态；避免一个 `isLoading` 控制所有行为。
- 刷新或加载更多失败时保留已有内容，并提供范围合适的重试入口。
- 搜索条件变化时取消或淘汰旧请求；通过条件快照或请求标识避免迟到结果覆盖当前状态。
- 按业务需要防抖搜索输入；不要在每次重组或每次输入变化时无控制地发起请求。
- 防止重复分页、重复提交和快速连点导致状态错乱。乐观更新必须有失败回滚或重新同步机制。
- 返回列表时尽量保留滚动位置和搜索条件；通过轻量状态保存恢复必要页面信息，不保存整份大型数据集。
- 支持系统返回、深色模式、字体缩放、系统栏与键盘 inset；不要靠锁定方向或固定屏幕尺寸解决布局问题。
- 列表项使用稳定 key；网格列数随可用宽度合理变化。
- 图标按钮提供有意义的无障碍说明；保证合理触控面积，不仅靠颜色表示状态。
- 加载失败、缺失媒体、不支持的媒体类型都有明确占位或提示，不能造成崩溃。
- 内部技术细节不直接作为用户错误文案；详细诊断只留在脱敏的开发日志中。

## 7. 媒体、性能与轻量化

- 使用图片库统一处理加载、解码、内存缓存和磁盘缓存，不自行重复实现完整图片框架。
- 列表加载与显示尺寸匹配的缩略图；用户进入详情或主动选择后再加载较大资源。
- 缓存、分页驻留数据和预取均设置边界，不无限累积内容。
- 不默认在列表中自动播放视频或同时解码大量动图；需要播放能力时按需引入并管理播放器生命周期。
- 页面离开后释放播放器和临时资源，取消不再需要的任务。
- 大文件采用流式处理，避免将完整内容一次性读入内存。
- 不默认启用后台轮询、全站同步或批量预下载。
- 新增依赖前检查平台与现有库能否完成任务，并说明新增库解决的具体问题。
- 不承诺未经测量的 APK 体积、帧率或启动时间。出现性能问题时，先定位，再针对性优化。
- 发布构建使用适合项目的代码与资源缩减设置，并验证反射、序列化和媒体加载未受影响。

## 8. 安全与本地数据

- 敏感凭据集中由安全存储组件管理，禁止写入源码、资源、BuildConfig、日志、导航参数、SavedStateHandle 或版本库。
- 默认提供仅会话使用的方式；需要持久化时，使用 Android Keystore 管理加密密钥，将敏感内容加密后存入应用私有空间。不要将任意文本凭据直接当作 Keystore 条目保存。
- DataStore 不应被视为天然加密存储；不得将明文敏感信息直接放入普通设置文件。
- 凭据相关文件应排除备份和设备迁移；处理密钥失效或恢复后无法解密的情况，提示用户重新配置，不能崩溃。
- 退出或切换账户时清除旧会话的内存状态与敏感缓存，取消旧账户任务，避免旧结果进入新会话。
- 网络与媒体加载的凭据边界必须明确，敏感请求头不能被附加到任意媒体地址、外部链接或跨域跳转。
- 不关闭 TLS 证书校验，不使用信任所有证书的实现，不为调试放开生产环境明文流量。
- 外链只允许预期的协议，由系统处理合适的外部页面；不要把远程内容当可执行 HTML/JavaScript 随意加载。
- 不默认引入分析追踪、广告 SDK 或敏感内容采集。只申请当前功能确实需要的权限。
- 本地缓存与历史提供可理解的清除入口；不承诺清除操作具备未实现的安全擦除效果。

密钥管理实现参考 [Android Keystore 官方文档](https://developer.android.com/privacy-and-security/keystore)，优先使用平台安全能力，不自行设计加密算法。

## 9. 构建与仓库维护

- 使用仓库 Gradle Wrapper 构建，保持 Wrapper、插件和 Version Catalog 的版本关系一致。
- 优先使用标准依赖仓库；不随意添加来源不明的 Maven 仓库或修改下载源。
- `.gitignore` 应排除构建产物、机器相关路径、敏感配置和签名材料；保留可复现构建必需的 Wrapper 文件。
- `local.properties` 中的本机 SDK 路径不得硬编码到共享构建脚本。
- 不提交生产密钥、签名文件或真实用户数据；测试样例和截图使用合成或脱敏内容。
- 不因开发环境缺少 SDK、模拟器或网络而删除依赖、绕过检查或改写真实业务行为。
- README 记录环境要求、运行步骤、实际实现的功能、必要配置与已知限制。接口协议保持在独立文档中。
- 不为了运行检查而升级整套工具链；必要升级需说明兼容性原因和影响范围。

## 10. 验证要求

每次修改按风险选择验证范围，优先覆盖可观察行为，不为简单样式、命名或文案修改编写机械测试。

重要业务逻辑的测试重点：

- 搜索条件切换后，旧响应不会覆盖新结果。
- 刷新、分页、去重、终止条件和失败重试的状态正确。
- 会话变化后，不继续展示或更新旧账户的私有状态。
- 关键操作成功、失败和重复点击时，页面与共享数据保持一致。
- 序列化能够处理真实存在的可选字段、空值、未知字段和错误响应；网络契约细节查阅独立接口文档。
- 缓存失效、缺失媒体和取消任务不会引起崩溃或无限循环。

使用 fake Repository 或可控网络测试服务完成自动化验证。测试不得依赖开发者真实账户，也不得在真实服务上自动执行写操作。UI 测试优先覆盖核心导航和重要交互，不追求没有业务价值的覆盖率数字。

对默认单模块项目，可执行：

```bash
./gradlew :app:assembleDebug :app:testDebugUnitTest :app:lintDebug
```

Windows PowerShell 对应：

```powershell
.\gradlew.bat :app:assembleDebug :app:testDebugUnitTest :app:lintDebug
```

仅在存在对应测试及设备时执行：

```bash
./gradlew :app:connectedDebugAndroidTest
```

若项目具有不同模块名、variant 或任务名称，应先确认实际任务并调整。不能声称未执行的测试已经通过。只在有具体风险或发布目标时增加 Release 构建、真机性能检查等验证。

## 11. Codex 工作方式

1. **先理解现状**：阅读适用的 AGENTS.md、README、构建配置和本次业务相关代码；涉及联网行为时再阅读独立 API 文档。
2. **确定最小交付**：围绕用户本次要求完成一个可运行闭环。功能范围未确定时，可先完成工程基础、主题与导航；不得把本文件中的包示例自动转成全部待开发功能。
3. **做合理决定**：文件名、组件拆分、局部 UI 等可逆选择按本文件执行，简要记录关键假设，不反复要求用户确认。
4. **先有具体进展**：缺少必要信息时先完成不受影响的部分；仅当信息会实质改变实现或无法安全继续时，提出一个明确问题。
5. **保持修改聚焦**：保留用户未提交的修改，不覆盖无关代码，不擅自重命名整个项目或调整应用身份。
6. **完成实现与验证**：不止提供计划；完成已授权任务、运行适当检查，修复本次修改引入的问题。
7. **同步必要说明**：架构、运行方式或配置改变时同步 README；不生成大量重复文档。
8. **诚实交付**：说明完成内容、涉及的主要文件、实际验证结果和剩余限制；明确区分环境阻塞、已有问题与本次新问题。

禁止行为：

- 编造 API、依赖版本、验证结果或已经完成的功能。
- 用硬编码假数据掩盖联网失败，或将 Preview/测试数据作为正式业务实现。
- 未获用户要求就扩展为上传、批量下载、后台同步等大功能。
- 为通过检查删除有意义的测试、压制全部 lint 问题或全面关闭安全检查。
- 未经明确授权执行破坏性 Git 操作、发布、签名分发或真实服务写入测试。

## 12. 完成标准

一项业务任务只有满足以下条件才可报告为完成：

- 当前需求的主要交互可运行，错误和空状态有处理。
- 新代码归属清晰，符合按业务分包和依赖边界要求。
- 无新增敏感数据泄露、明显资源泄漏、无限请求或重复任务问题。
- 在可用环境中完成必要构建和测试；未验证部分已明确说明。
- 不残留冒充正式功能的占位实现，必要使用说明已更新。

## 13. 杂项

- app默认的主色为#012e56，副色为#fcbf31，该设置可有用户后期自定义。
